<div class="language-selector">
    <%= link_to t('layouts.language.english'), url_for(locale: :en) %>
    <%= link_to t('layouts.language.ukrainian'), url_for(locale: :uk) %>
</div>

<div class="container">
  <div class="row justify-content-center align-items-center min-vh-100">
    <div class="col-md-6">
      <div class="text-center">
        <h1><%= t('menu.header')%></h1>
        <% @pdf_files.each do |pdf_file| %>
          <%
=begin%>
 <%= link_to pdf_file.name, pdf_file.file_url, class: "btn btn-primary btn-block btn-lg mb-2", target: "_blank" %> 
<%
=end%>
          <%
=begin%>
 <%= link_to pdf_file.name, open_pdf_path(pdf_file), class: "btn btn-primary btn-block btn-lg mb-2", target: "_blank" %> 
<%
=end%>
          <%= link_to pdf_file.name, pdf_file.file_url, class: "btn btn-primary btn-block btn-lg mb-2 open-pdf-link", target: "_blank" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
