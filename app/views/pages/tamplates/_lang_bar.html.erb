<% I18n.available_locales.map { |l| [I18n.t(:language, locale: l), l.to_s] }.each do |local| %>
  <%= link_to local.first, url_for(locale: local.second), class: "btn #{ I18n.locale.to_s == local.second ? 'btn-dark' : 'btn-secondary'}" %>
<% end %>